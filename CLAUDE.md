## Agent skills

### Issue tracker

Issues live in GitHub Issues for `hivtools/leapfrog-compare`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — one root `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.
